# Deployment — Frontend Demo Mode

This temporary build does not connect to Supabase. Deploy it as a normal Netlify build.

- Build command: `npm run build`
- Publish directory: `dist/client`
- Base directory: blank
- Environment variables: none required

Orders, settings, menu edits and demo authentication are stored in browser localStorage.
