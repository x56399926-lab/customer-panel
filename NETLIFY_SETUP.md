# Netlify — Frontend Demo Mode

This build is intentionally backend-free for UI preview. It uses localStorage and seeded mock data.

Build command: `npm run build`
Publish directory: `dist/client`

No Supabase environment variables are required.

Demo admin login: use an email containing `admin`, e.g. `admin@demo.local`, with any password.
Demo customer login: any email + password.
